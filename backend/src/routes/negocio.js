import { Router } from 'express';

const router = Router();

// Todas las rutas bajo /api/negocios/:negocioId/...
// Unidades    — S-3
// Categorías  — S-3
// Proveedores — S-3
// Insumos     — S-4
// Productos   — D-1
// BOM         — D-1
// Etapas      — D-1
// Fichas      — D-2
// Lotes       — L-3
// Bitácora    — L-3

export default router;
